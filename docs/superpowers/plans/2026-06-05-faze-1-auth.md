# Fáze 1 — Trvalý účet (email+heslo+Google): Implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Přidat self-service registraci a přihlášení (email+heslo + Google OAuth) s automatickým vznikem profilu, jedním auth vstupem a trvalou session — základ pro samoobslužné hlášení na závod (Fáze 2).

**Architecture:** DB trigger `handle_new_user` zakládá profil po vzniku auth usera (email i Google). Server actions v `auth.actions.ts` obalují Supabase Auth (signUp/OAuth/reset). Nový route handler `/auth/callback` dokončuje OAuth code-flow. UI: jeden vstup `/login` + `/registrace` + reset stránky ve stylu stávajícího `(auth)/login`. Bez ověřování emailu (Supabase „Confirm email" OFF). Čisté validátory testované vitestem (TDD).

**Tech Stack:** Next.js 15 App Router, TypeScript, `@supabase/ssr`, Supabase Auth (password + Google), vitest 4.

**Pořadí nasazení:** DB migrace 018 (Dušan ručně) může jít kdykoli (trigger je aditivní). TS přes GitHub→Vercel. Google OAuth funguje až po konfiguraci provideru v Supabase + Google Cloud (Dušanova akce, Task 10).

**Předpoklad:** Dušan smaže stávající Auth users (čistý start) a v Supabase nastaví Confirm email OFF + SMTP Resend (viz spec §10).

---

## File Structure

| Soubor | Odpovědnost | Akce |
|--------|-------------|------|
| `supabase/migrations/018_handle_new_user.sql` + `_rollback.sql` | Trigger vzniku profilu | Create |
| `src/lib/validators/auth.ts` + `__tests__/auth.test.ts` | Čistá validace email/heslo/jméno | Create |
| `src/actions/auth.actions.ts` | `signUpWithPassword`, `signInWithGoogle` (vrací URL), `requestPasswordReset`, `updatePassword`, `resolveLandingPath` | Modify |
| `src/app/auth/callback/route.ts` | OAuth code exchange | Create |
| `src/app/(auth)/login/page.tsx` | Přestavba: email+heslo + Google + odkazy | Modify |
| `src/app/(auth)/registrace/page.tsx` | Registrace email+heslo + Google + GDPR | Create |
| `src/app/(auth)/zapomenute-heslo/page.tsx` | Žádost o reset | Create |
| `src/app/(auth)/reset-hesla/page.tsx` | Nastavení nového hesla | Create |
| `src/app/admin/login/page.tsx` | Redirect na `/login` (sjednocení) | Modify |
| `src/middleware.ts` | Public routes + admin/login redirect | Modify |
| `src/lib/constants.ts` | Odstranit `SESSION_TIMEOUT_HOURS` | Modify |

---

## Task 1: DB trigger `handle_new_user` (migrace 018)

**Files:** Create `supabase/migrations/018_handle_new_user.sql`, `supabase/migrations/018_rollback.sql`

- [ ] **Step 1: Napsat migraci**

```sql
-- 018_handle_new_user.sql
-- Po vzniku auth usera (email signup i Google OAuth) automaticky založí profil.
-- Idempotentní (ON CONFLICT DO NOTHING) — nekoliduje s pořadatelovým admin-create upsertem.
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.profiles (id, email, jmeno, terms_accepted_at, privacy_policy_version)
  VALUES (
    NEW.id,
    NEW.email,
    COALESCE(
      NULLIF(NEW.raw_user_meta_data->>'jmeno', ''),
      NULLIF(NEW.raw_user_meta_data->>'full_name', ''),
      NULLIF(NEW.raw_user_meta_data->>'name', ''),
      split_part(NEW.email, '@', 1)
    ),
    CASE WHEN (NEW.raw_user_meta_data->>'terms_accepted') = 'true' THEN NOW() ELSE NULL END,
    CASE WHEN (NEW.raw_user_meta_data->>'terms_accepted') = 'true' THEN to_char(NOW(), 'YYYY-MM-DD') ELSE NULL END
  )
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = public;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

- [ ] **Step 2: Napsat rollback**

```sql
-- 018_rollback.sql
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
DROP FUNCTION IF EXISTS public.handle_new_user();
```

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/018_handle_new_user.sql supabase/migrations/018_rollback.sql
git commit -m "feat(auth): migrace 018 — trigger handle_new_user pro vznik profilu"
```

Pozn.: migraci pouští Dušan ručně v Supabase SQL editoru (jako 016/017). Lze kdykoli — je aditivní.

---

## Task 2: Validátory auth (TDD)

**Files:** Create `src/lib/validators/auth.ts`, `src/lib/validators/__tests__/auth.test.ts`

- [ ] **Step 1: Failing test**

```typescript
// src/lib/validators/__tests__/auth.test.ts
import { describe, it, expect } from 'vitest'
import { validateEmail, validatePassword, validateJmeno } from '../auth'

describe('validateEmail', () => {
  it('přijme platný email', () => { expect(validateEmail('a@b.cz').ok).toBe(true) })
  it('odmítne bez @', () => { expect(validateEmail('ab.cz').ok).toBe(false) })
  it('odmítne prázdný', () => { expect(validateEmail('').ok).toBe(false) })
})
describe('validatePassword', () => {
  it('přijme heslo >= 8 znaků', () => { expect(validatePassword('heslo123').ok).toBe(true) })
  it('odmítne krátké', () => { expect(validatePassword('abc').ok).toBe(false) })
})
describe('validateJmeno', () => {
  it('přijme jméno', () => { expect(validateJmeno('Jan Novák').ok).toBe(true) })
  it('odmítne prázdné/krátké', () => { expect(validateJmeno('J').ok).toBe(false) })
})
```

- [ ] **Step 2: Run — fail**

Run: `npm test`
Expected: FAIL — `Cannot find module '../auth'`.

- [ ] **Step 3: Implementace**

```typescript
// src/lib/validators/auth.ts
export type Validation = { ok: true } | { ok: false; reason: string }
const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/

export function validateEmail(email: string): Validation {
  if (!email || !EMAIL_RE.test(email)) return { ok: false, reason: 'Neplatný email' }
  return { ok: true }
}
export function validatePassword(password: string): Validation {
  if (!password || password.length < 8) return { ok: false, reason: 'Heslo musí mít alespoň 8 znaků' }
  return { ok: true }
}
export function validateJmeno(jmeno: string): Validation {
  if (!jmeno || jmeno.trim().length < 2) return { ok: false, reason: 'Zadej jméno' }
  return { ok: true }
}
```

- [ ] **Step 4: Run — pass**

Run: `npm test`
Expected: PASS (8 testů).

- [ ] **Step 5: Commit**

```bash
git add src/lib/validators/auth.ts src/lib/validators/__tests__/auth.test.ts
git commit -m "feat(auth): validátory email/heslo/jméno (TDD)"
```

---

## Task 3: Server actions (registrace, Google, reset, role redirect)

**Files:** Modify `src/actions/auth.actions.ts` (přidat funkce; soubor má `// @ts-nocheck` a `'use server'`)

- [ ] **Step 1: Přidat funkce na konec souboru**

Importy nahoře už obsahují `createClient` (server) a `createAdminClient`. Přidej (pokud chybí) import validátorů a `is_system_admin` se volá přes RPC.

```typescript
import { validateEmail, validatePassword, validateJmeno } from '@/lib/validators/auth'

/** Registrace email+heslo. Bez email confirm → rovnou session. Profil založí DB trigger. */
export async function signUpWithPassword(input: {
  jmeno: string; email: string; heslo: string; termsAccepted: boolean
}): Promise<ActionResult<{ userId: string }>> {
  try {
    if (!input.termsAccepted) return { success: false, error: { code: ErrorCodes.INVALID_INPUT, message: 'Musíš souhlasit s podmínkami' } }
    for (const v of [validateJmeno(input.jmeno), validateEmail(input.email), validatePassword(input.heslo)]) {
      if (!v.ok) return { success: false, error: { code: ErrorCodes.INVALID_INPUT, message: v.reason } }
    }
    const supabase = await createClient()
    const { data, error } = await supabase.auth.signUp({
      email: input.email,
      password: input.heslo,
      options: { data: { jmeno: input.jmeno.trim(), terms_accepted: 'true' } },
    })
    if (error) return { success: false, error: { code: ErrorCodes.DATABASE_ERROR, message: error.message } }
    return { success: true, data: { userId: data.user!.id } }
  } catch (e) {
    return { success: false, error: toErrorResponse(e) }
  }
}

/** Vrátí URL pro přesměrování na Google OAuth (volá se ze server action, redirect dělá klient). */
export async function signInWithGoogle(redirectPath: string = '/'): Promise<ActionResult<{ url: string }>> {
  try {
    const supabase = await createClient()
    const origin = (await headers()).get('origin') ?? ''
    const { data, error } = await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: { redirectTo: `${origin}/auth/callback?next=${encodeURIComponent(redirectPath)}` },
    })
    if (error || !data?.url) return { success: false, error: { code: ErrorCodes.DATABASE_ERROR, message: error?.message ?? 'OAuth selhalo' } }
    return { success: true, data: { url: data.url } }
  } catch (e) {
    return { success: false, error: toErrorResponse(e) }
  }
}

/** Pošle email s odkazem pro reset hesla. */
export async function requestPasswordReset(email: string): Promise<ActionResult<{ message: string }>> {
  try {
    const supabase = await createClient()
    const origin = (await headers()).get('origin') ?? ''
    const { error } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${origin}/reset-hesla` })
    if (error) return { success: false, error: { code: ErrorCodes.DATABASE_ERROR, message: error.message } }
    return { success: true, data: { message: 'Pokud email existuje, poslali jsme odkaz pro obnovu hesla.' } }
  } catch (e) {
    return { success: false, error: toErrorResponse(e) }
  }
}

/** Nastaví nové heslo (v recovery session po kliknutí na odkaz). */
export async function updatePassword(newPassword: string): Promise<ActionResult> {
  try {
    const v = validatePassword(newPassword)
    if (!v.ok) return { success: false, error: { code: ErrorCodes.INVALID_INPUT, message: v.reason } }
    const supabase = await createClient()
    const { error } = await supabase.auth.updateUser({ password: newPassword })
    if (error) return { success: false, error: { code: ErrorCodes.DATABASE_ERROR, message: error.message } }
    return { success: true }
  } catch (e) {
    return { success: false, error: toErrorResponse(e) }
  }
}

/** Po loginu: kam přesměrovat dle role (admin/pořadatel → /admin, jinak → /). */
export async function resolveLandingPath(): Promise<ActionResult<{ path: string }>> {
  try {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return { success: true, data: { path: '/' } }
    if (isSystemAdmin(user.id)) return { success: true, data: { path: '/admin' } }
    const { data: isSys } = await supabase.rpc('is_system_admin' as any, { p_user_id: user.id })
    if (isSys === true) return { success: true, data: { path: '/admin' } }
    const { data: roles } = await supabase.from('zavod_role').select('role').eq('user_id', user.id).in('role', ['hlavni_admin', 'poradatel'])
    return { success: true, data: { path: (roles && roles.length > 0) ? '/admin' : '/' } }
  } catch {
    return { success: true, data: { path: '/' } }
  }
}
```

Ověř, že `headers` je importováno z `next/headers` (přidej `import { headers } from 'next/headers'`, pokud chybí) a `isSystemAdmin` z `@/lib/constants`. `ErrorCodes.INVALID_INPUT` ověř v `src/lib/errors.ts` — pokud neexistuje, použij nejbližší (`VALIDATION_ERROR`/`MISSING_PHOTO` styl) a nahlas.

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: build projde.

- [ ] **Step 3: Commit**

```bash
git add src/actions/auth.actions.ts
git commit -m "feat(auth): server actions signUp/Google/reset/resolveLandingPath"
```

---

## Task 4: OAuth callback route

**Files:** Create `src/app/auth/callback/route.ts`

- [ ] **Step 1: Route handler**

```typescript
// src/app/auth/callback/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/'
  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) return NextResponse.redirect(`${origin}${next}`)
  }
  return NextResponse.redirect(`${origin}/login?error=oauth`)
}
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: build projde, route `/auth/callback` se objeví ve výpisu.

- [ ] **Step 3: Commit**

```bash
git add src/app/auth/callback/route.ts
git commit -m "feat(auth): OAuth callback route /auth/callback"
```

---

## Task 5: middleware — public routes + sjednocení admin/login

**Files:** Modify `src/middleware.ts`

- [ ] **Step 1: Rozšířit `publicRoutes`**

V poli `publicRoutes` (řádky 22–31) přidej nové routy:
```typescript
const publicRoutes = [
  '/login',
  '/admin/login',
  '/registrace',
  '/zapomenute-heslo',
  '/reset-hesla',
  '/auth/callback',
  '/zavod/*/verejnost',
  '/zavod/*/leaderboard',
  '/zavod/*/galerie',
  '/zavod/*/pravidla',
  '/archiv',
  '/pozvanka/*',
]
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: build projde.

- [ ] **Step 3: Commit**

```bash
git add src/middleware.ts
git commit -m "feat(auth): public routes pro registraci/reset/oauth callback"
```

---

## Task 6: Přestavba `/login` (email+heslo + Google + odkazy)

**Files:** Modify `src/app/(auth)/login/page.tsx`

Zachovej `'use client'`, Suspense wrapper kvůli `useSearchParams`, `returnTo` (default `/`) a vizuální styl (syrové Tailwind inputy jako dnes — `(auth)/layout.tsx` dává pozadí). Nahraď OTP dvoukrok za email+heslo.

- [ ] **Step 1: Přepsat formulářovou logiku**

Importy akcí: `import { signInWithPassword, signInWithGoogle, resolveLandingPath } from '@/actions/auth.actions'`. Stav: `email`, `password`, `error`, `isPending` (přes `useTransition`).

Submit handler:
```typescript
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault()
  setError(null)
  startTransition(async () => {
    const result = await signInWithPassword(email, password)
    if (!result.success) { setError(result.error?.message || 'Přihlášení se nezdařilo'); return }
    // returnTo má přednost; jinak dle role
    let dest = returnTo
    if (returnTo === '/') {
      const landing = await resolveLandingPath()
      if (landing.success) dest = landing.data!.path
    }
    router.push(dest); router.refresh()
  })
}

const handleGoogle = () => {
  startTransition(async () => {
    const result = await signInWithGoogle(returnTo)
    if (result.success && result.data?.url) { window.location.href = result.data.url }
    else setError(result.error?.message || 'Přihlášení přes Google selhalo')
  })
}
```

- [ ] **Step 2: UI**

Formulář: `<input type="email">` (email) + `<input type="password">` (heslo), submit „Přihlásit se". Pod ním:
- tlačítko `Přihlásit přes Google` → `onClick={handleGoogle}` (styl secondary/outline, ikona Google nebo text).
- odkaz `<Link href="/registrace">Nemáš účet? Zaregistruj se</Link>`
- odkaz `<Link href="/zapomenute-heslo">Zapomenuté heslo?</Link>`
- error box (červený, jako dnes na řádcích 99–103).

Použij stávající Tailwind třídy ze souboru pro inputy/tlačítka (konzistence). Odeber veškerou OTP logiku (`step`, `code`, `generateLoginCode`, `verifyLoginCode`, `handleBackToEmail`).

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: build projde, `/login` se renderuje.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(auth)/login/page.tsx"
git commit -m "feat(auth): /login na email+heslo + Google + odkazy"
```

---

## Task 7: Stránka `/registrace`

**Files:** Create `src/app/(auth)/registrace/page.tsx`

`'use client'`. Stejný vizuální styl jako `(auth)/login` (využívá `(auth)/layout.tsx`). Pole: jméno, email, heslo, GDPR checkbox.

- [ ] **Step 1: Stránka**

```typescript
'use client'
import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { signUpWithPassword, signInWithGoogle, resolveLandingPath } from '@/actions/auth.actions'

export default function RegistracePage() {
  const router = useRouter()
  const [jmeno, setJmeno] = useState('')
  const [email, setEmail] = useState('')
  const [heslo, setHeslo] = useState('')
  const [terms, setTerms] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [isPending, startTransition] = useTransition()

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault(); setError(null)
    startTransition(async () => {
      const r = await signUpWithPassword({ jmeno, email, heslo, termsAccepted: terms })
      if (!r.success) { setError(r.error?.message || 'Registrace selhala'); return }
      const landing = await resolveLandingPath()
      router.push(landing.success ? landing.data!.path : '/'); router.refresh()
    })
  }
  const handleGoogle = () => {
    if (!terms) { setError('Musíš souhlasit s podmínkami'); return }
    startTransition(async () => {
      const r = await signInWithGoogle('/')
      if (r.success && r.data?.url) window.location.href = r.data.url
      else setError(r.error?.message || 'Google registrace selhala')
    })
  }
  // ... JSX: inputy jméno/email/heslo (stejný Tailwind styl jako (auth)/login),
  // checkbox terms s odkazy na /podminky-uziti a /ochrana-osobnich-udaju,
  // tlačítko "Registrovat se" (submit), tlačítko "Registrovat přes Google" (handleGoogle),
  // error box, odkaz "Máš účet? Přihlas se" → /login.
  // Submit i Google tlačítko disabled při isPending.
}
```

JSX doplň ve stylu `src/app/(auth)/login/page.tsx` (syrové `<input>`/`<button>` s týmiž Tailwind třídami). GDPR: nativní `<input type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)} required>` + label s odkazy. Google tlačítko vyžaduje `terms` (kontrola v `handleGoogle`).

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: build projde, `/registrace` se renderuje.

- [ ] **Step 3: Commit**

```bash
git add "src/app/(auth)/registrace/page.tsx"
git commit -m "feat(auth): registrační stránka /registrace"
```

---

## Task 8: Reset hesla — `/zapomenute-heslo` + `/reset-hesla`

**Files:** Create `src/app/(auth)/zapomenute-heslo/page.tsx`, `src/app/(auth)/reset-hesla/page.tsx`

- [ ] **Step 1: `/zapomenute-heslo`**

```typescript
'use client'
import { useState, useTransition } from 'react'
import Link from 'next/link'
import { requestPasswordReset } from '@/actions/auth.actions'

export default function ZapomenuteHesloPage() {
  const [email, setEmail] = useState('')
  const [message, setMessage] = useState<string | null>(null)
  const [error, setError] = useState<string | null>(null)
  const [isPending, startTransition] = useTransition()
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault(); setError(null); setMessage(null)
    startTransition(async () => {
      const r = await requestPasswordReset(email)
      if (r.success) setMessage(r.data!.message); else setError(r.error?.message || 'Chyba')
    })
  }
  // JSX: input email, tlačítko "Poslat odkaz", message/error box, odkaz zpět na /login.
  // Styl jako (auth)/login.
}
```

- [ ] **Step 2: `/reset-hesla`**

Po kliknutí na odkaz z emailu má uživatel recovery session (Supabase nastaví přes URL). Stránka jen nastaví nové heslo.

```typescript
'use client'
import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { updatePassword } from '@/actions/auth.actions'

export default function ResetHeslaPage() {
  const router = useRouter()
  const [heslo, setHeslo] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [done, setDone] = useState(false)
  const [isPending, startTransition] = useTransition()
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault(); setError(null)
    startTransition(async () => {
      const r = await updatePassword(heslo)
      if (r.success) { setDone(true); setTimeout(() => router.push('/login'), 1500) }
      else setError(r.error?.message || 'Nepodařilo se změnit heslo')
    })
  }
  // JSX: input type=password "Nové heslo", tlačítko "Změnit heslo", done/error box.
  // Styl jako (auth)/login.
}
```

Pozn.: recovery session funguje, protože odkaz z emailu obsahuje token, který `@supabase/ssr` middleware/klient zpracuje. Pokud by `updateUser` hlásil chybějící session, doplň na začátek stránky `createClient().auth.getSession()` kontrolu (implementační detail — ověř při testu).

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: build projde, obě routy se renderují.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(auth)/zapomenute-heslo/page.tsx" "src/app/(auth)/reset-hesla/page.tsx"
git commit -m "feat(auth): reset hesla — zapomenute-heslo + reset-hesla"
```

---

## Task 9: Sjednotit admin login + session cleanup

**Files:** Modify `src/app/admin/login/page.tsx`, `src/lib/constants.ts`, `src/middleware.ts`

- [ ] **Step 1: `/admin/login` → redirect na `/login`**

Nahraď obsah `src/app/admin/login/page.tsx` za přesměrování (server komponenta):
```typescript
import { redirect } from 'next/navigation'
export default function AdminLoginRedirect() {
  redirect('/login')
}
```

- [ ] **Step 2: middleware — admini na `/login`**

V `src/middleware.ts` v bloku redirectu nepřihlášených (řádky ~94–104) změň větev pro `hlavniAdminRoutes`, aby šla na `/login` s returnTo (ne `/admin/login`):
```typescript
if (!user) {
  const loginUrl = new URL('/login', request.url)
  loginUrl.searchParams.set('returnTo', pathname)
  return NextResponse.redirect(loginUrl)
}
```
(Odeber speciální větev `if (matchRoute(pathname, hlavniAdminRoutes)) → /admin/login`.)

- [ ] **Step 3: Odstranit `SESSION_TIMEOUT_HOURS`**

V `src/lib/constants.ts` odstraň konstantu `SESSION_TIMEOUT_HOURS` (řádky ~42–46) a její komentář. Ověř, že se nikde nepoužívá: `grep -rn "SESSION_TIMEOUT_HOURS" src/` → musí být prázdné po odstranění.

- [ ] **Step 4: Build**

Run: `npm run build`
Expected: build projde.

- [ ] **Step 5: Commit**

```bash
git add src/app/admin/login/page.tsx src/lib/constants.ts src/middleware.ts
git commit -m "feat(auth): sjednotit admin login na /login + zrušit session timeout"
```

---

## Task 10: CHECKPOINT — build/test + Dušan dashboard (Google + Confirm OFF + SMTP)

**Files:** žádné (verifikace + konfigurace)

- [ ] **Step 1: Build + testy**

Run: `npm run build && npm test`
Expected: build OK, testy zelené (validátory).

- [ ] **Step 2: Dušan — Supabase + Google config**

(Mimo kód — controller předá Dušanovi.) Smazat Auth users; Confirm email OFF; SMTP Resend; **Google provider:** Google Cloud Console → OAuth 2.0 Client ID (Web), Authorized redirect URI = `https://<projekt>.supabase.co/auth/v1/callback`; Client ID/Secret vložit do Supabase → Authentication → Providers → Google; do Supabase → URL Configuration přidat redirect `https://carpclub.app/auth/callback` (+ `http://localhost:3000/auth/callback` pro dev).

- [ ] **Step 3: Push + deploy**

```bash
git push -u origin feat/faze-1-auth
```
Po code review merge do `main` → Vercel deploy. (Migraci 018 pustí Dušan.)

---

## Task 11: Verifikace E2E + regrese + merge

**Files:** žádné (verifikace)

- [ ] **Step 1: Trigger — vznik profilu**

Po migraci 018: registrace přes `/registrace` → ověř v Supabase, že vznikl řádek v `profiles` se správným `jmeno` a `terms_accepted_at`.

- [ ] **Step 2: Flow ověření (lokálně/preview)**

- Registrace email+heslo → rovnou přihlášen → profil existuje.
- Login email+heslo na `/login`.
- Reset hesla: `/zapomenute-heslo` → email → `/reset-hesla` → nové heslo → login.
- Google (po konfiguraci): `/login` → Google → `/auth/callback` → session → profil.
- `resolveLandingPath`: admin po loginu → `/admin`, rybář → `/`.

- [ ] **Step 3: Regrese**

- Pozvánkový flow: `createPozvanka` → `/pozvanka/[token]` → login funguje.
- `/admin` chráněná: nepřihlášený → `/login?returnTo=/admin`; po loginu admina → `/admin`.
- `/admin/login` → redirect na `/login`.

- [ ] **Step 4: Merge + paměť**

```bash
git checkout main && git merge --ff-only feat/faze-1-auth && git push origin main
```
Aktualizovat `~/.claude/.../memory/carp_club_app.md` — Fáze 1 nasazena.

---

## Self-Review

**Spec coverage:** §3 UI vstupy → T6/T7/T8/T9; §4 server actions → T3; §5 trigger → T1; §6 OAuth callback → T4; §7 session → T9 (SESSION_TIMEOUT_HOURS); §8 GDPR → T1 (trigger terms) + T7 (checkbox); §9 default role → beze změny (divák); §10 dashboard → T10; §11 testování → T11; validátory → T2; middleware public routes → T5. **Pokryto.**

**Placeholder scan:** UI JSX u T6/T7/T8 odkazuje na styl `(auth)/login` se zadanou strukturou prvků a doslovnými handlery — ne „TODO", ale konkrétní vzor + kompletní logika. Server actions, trigger, callback, middleware = doslovný kód.

**Type consistency:** `ActionResult`, `ErrorCodes`, `signInWithPassword`, `resolveLandingPath`, `signInWithGoogle(redirectPath)→{url}`, `signUpWithPassword({jmeno,email,heslo,termsAccepted})` konzistentní napříč T3/T6/T7. `is_system_admin` RPC + `isSystemAdmin` (env) jako ve Fázi 0.

**Předpoklady k ověření za běhu:** `ErrorCodes.INVALID_INPUT` ověřit v `errors.ts` (T3 Step 1); recovery session pro `/reset-hesla` (T8 Step 2); `headers()` import z `next/headers`.
